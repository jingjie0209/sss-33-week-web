# 波西和皮普闪卡目录比对

来源：用户提供的夸克分享链接，目录 `波西皮普闪卡/TPR音频`。

## 目录结果

- TPR 音频：40 个 MP3，另有 1 个教学 PDF。
- 现有项目卡片：120 张。
- 接入后的项目卡片：141 张（新增 21 张动作卡）。

## 已有卡片的同名音频（14）

`build blocks`、`cry`、`drive`、`feed`、`hide`、`lie down`、`mop the floor`、`pour`、`pull`、`push`、`sleep`、`stir`、`take a bath`、`wash hands`

## 已有短语的音频变体（5）

| 分享文件 | 项目卡片 |
| --- | --- |
| `kick.mp3` | `kick the ball` |
| `drink.mp3` | `drink water` |
| `slide.mp3` | `slide down` |
| `walk.mp3` | `walk down the stairs` |
| `ve hand.mp3` | `wave hand`（分享文件名疑似截断） |

`read.mp3` 同时作为新增的 `read` 卡音频，并可兼容现有 `read book` 卡。

## 新增动作卡（21）

`watch`、`stand`、`smile`、`run`、`sit`、`scare`、`put on`、`read`、`point`、`paint`、`hug`、`laugh`、`hold`、`give`、`have dinner`、`fly`、`chase`、`drag`、`climb`、`blow`、`take off shoes`

## 音频接入说明

每张卡已写入对应的 `audioFile` 文件名。由于夸克下载接口要求登录，且当前分享页显示账号空间为 0B、功能受限，项目没有复制分享中的 MP3。详情页点击英文会优先尝试 `assets/posy-pip-audio/` 下同名 MP3；文件不存在时自动使用浏览器英文发音，保证点击操作仍可用。
